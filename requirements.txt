# AI Pre-Scan — Python 3.12
# Versions are those verified present in the bootcamp-env conda environment on 10 August 2026.

# Orchestration — the primary stack
langgraph>=1.2.10
langchain>=1.3.14
langchain-core>=1.5.2
langchain-openai>=1.4.1

# Models and retrieval
openai>=2.45.0
pinecone>=7.3.0          # the current package; `pinecone-client` is retired, do not add it

# Tools and fetching
requests>=2.34.2
httpx>=0.28.1
beautifulsoup4>=4.15.0

# Reliability and contracts
tenacity>=9.1.4          # exponential backoff with jitter on external calls
pydantic>=2.13.4         # structured extraction; malformed output is rejected, not guessed

# Blocked-host fallback — bot detection on some hosts is intermittent, which makes recall vary
# between runs unless a real browser can be used as a backstop.
playwright>=1.40            # after install: python -m playwright install chromium

# Configuration
python-dotenv>=1.2.2     # reads the shared Ironhack key store; no keys live in this repo

# Tests. Listed here because the README documents `pytest` as a run step, and a clean install that
# cannot execute its own documented command is a broken setup — found by cloning into an empty
# virtualenv rather than by trusting the working one.
pytest>=8.0
